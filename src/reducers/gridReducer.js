const intialState = {
    currentCells: [],
    nextCells: []
}

const gridReducer = (state=intialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}

export default gridReducer;