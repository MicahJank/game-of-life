


// action creators

// updates the grid with the new grid
const updateGrid = () => dispatch => {
    
}

const generateNextGrid =() => dispatch => {

}

export default {
    updateGrid,
    generateNextGrid,
    // generateInitialGrid
}